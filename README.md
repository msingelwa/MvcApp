How to run the project:
I used the Code-First approach, so the app can create the database for you.

After opening on Visual Studio, open the terminal and run the below command
dotnet ef database update
